# Phase 3D 真机稳定性验收状态

更新时间：2026-08-22

## 已确认

- Windows 已识别 EasyInput `VID 303A / PID 1006`，输入桥返回 `boardConnected: true`。
- 真实板子语音键发送 `Ctrl + Shift + Space`，只读 Raw Input 诊断捕获了完整按下/释放序列；DeskMate 默认全局快捷键与页面按钮共用同一录音状态机。
- 电脑麦克风、千问 `qwen3-asr-flash`、历史保存和剪贴板输出已由用户实测可用。
- F22 输入桥兼容路径继续保留，并有自动化覆盖释放触发、防抖、重复事件、断线复位和辅助进程重启。

## 本阶段验收记录

- 真机设备枚举：通过。
- 真机语音键组合键：通过，实际为 `Ctrl+Shift+Space`。
- 10 次连续录音：待本阶段打包版人工计数确认。
- 权限拒绝、麦克风拔出、板子拔插、托盘和当前窗口输出：待本阶段逐项确认。
- API Key、录音、识别正文、窗口标题、完整设备路径和网络地址：不记录、不提交。

## 明确边界

- 当前录音源仍是电脑麦克风；没有 USB Audio 端点证据。
- EasyInput 板载 Wi-Fi 音频协议仍未确认，不扫描局域网、不猜测端口、不伪造连接。
- Codex、Claude Code、Hermes、WorkBody 仍为 mock 状态适配器，不代表真实 provider 已连接。
- 桌宠屏幕、灯效、舵机和传感器不在本阶段。

## 下一步人工验收

1. 启动 `release/win-unpacked/DeskMate.exe`，保持记事本或其他输入框为目标窗口。
2. 完成 10 次短录音，分别记录成功/失败计数，不记录文字正文。
3. 单独验证取消、拒绝麦克风权限、拔出麦克风、录音中拔出板子、最小化到托盘和千问失败回退。
4. 导出脱敏诊断，确认只含状态、来源、错误类型和耗时。
