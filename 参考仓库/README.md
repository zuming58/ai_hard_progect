# 参考仓库

这里保留项目依赖的外部技术参考仓库，作为事实和工具链依据：

- `easyinput-board-cy/`：EasyInput V2.0 板级合同、GPIO、电源和启动恢复资料。
- `esp-idf-cy/`：ESP-IDF 环境、构建、烧录和验证流程资料。

当前工作副本中两个目录各自保留原有 Git 历史和远端地址；顶层仓库记录它们的当前文件内容，便于多电脑完整克隆资料。

从顶层仓库克隆到另一台电脑时，得到的是完整文件快照，不会自动得到这两个参考仓库的内部 `.git` 历史；需要单独维护上游历史时，再在对应目录重新添加远端。
